const initialState = {
    team: {},
    teams: [],
    league: {},
    leagues: []
}

export default (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}